# 推送到 GitHub 指南

## 📋 推送前准备

### 1. 确认当前状态

```bash
# 查看当前修改的文件
git status

# 查看远程仓库配置
git remote -v
```

### 2. 添加要提交的文件

```bash
# 添加所有修改的文件（不包括 .gitignore 中的文件）
git add .

# 或者只添加特定文件
git add README.md src/app.tsx src/app.scss src/manifest.json
```

### 3. 提交更改

```bash
# 提交更改，并添加描述信息
git commit -m "更新：完成指令提示終端機功能開發

- 新增詳細的指令說明表格
- 實作即時 tooltip 提示
- 支援命令歷史和 Tab 補齊
- 優化頁面滾動和邊界處理
- 更新 README.md 文檔"
```

## 🚀 推送到 GitHub

### 方法一：强制推送（如果远程仓库是空的或你想覆盖）

⚠️ **注意：** 只有在确定要覆盖远程仓库时才使用此方法！

```bash
# 强制推送到 main 分支
git push -f origin main
```

### 方法二：合并后推送（推荐）

如果远程仓库已有内容，建议先合并：

```bash
# 1. 先拉取远程更改
git pull origin main --allow-unrelated-histories

# 2. 如果有冲突，解决冲突后：
git add .
git commit -m "合并远程更改"

# 3. 推送到远程
git push origin main
```

### 方法三：创建新分支推送（最安全）

```bash
# 1. 创建新分支
git checkout -b feature/teaching-terminal

# 2. 推送新分支
git push origin feature/teaching-terminal

# 3. 在 GitHub 上创建 Pull Request 合并到 main
```

## ✅ 推送后验证

1. 访问 https://github.com/tsai0120/linuxFinal_TeachingTerminal
2. 确认所有文件都已上传
3. 确认 README.md 显示正确

## 🔄 后续更新

如果之后需要更新代码：

```bash
# 1. 修改文件后
git add .
git commit -m "描述你的更改"
git push origin main
```

## 📝 注意事项

- **不要提交** `dist/`、`node_modules/` 等编译产物（已在 .gitignore 中）
- **每次编译前**记得执行 `cp src/manifest.json ./`
- 提交前建议运行 `npm run eslint` 检查代码

